# Contributing to AI Diary

AI Diaryプロジェクトへの貢献に興味を持っていただきありがとうございます！

## 開発環境のセットアップ

詳細な手順は [README.md](README.md) を参照してください。

### クイックスタート

```bash
# リポジトリをクローン
git clone https://github.com/yukiharmonica/ai-diary.git
cd ai-diary

# サーバー側: Dockerサービスを起動
cd infrastructure
docker compose up -d

# ローカルPC: VS CodeでSSH接続後、web-appをDev Containerで開く
# composer dev で開発サーバー起動
```

## 開発ワークフロー

### 1. ブランチ戦略

- `main` - 本番環境用の安定版
- `develop` - 開発用ブランチ
- `feature/*` - 新機能開発用
- `fix/*` - バグ修正用

### 2. コミットメッセージ規約

[Conventional Commits](https://www.conventionalcommits.org/) を使用してください：

```
<type>: <description>

[optional body]

[optional footer]
```

**Type:**
- `feat`: 新機能
- `fix`: バグ修正
- `docs`: ドキュメントのみの変更
- `style`: コードの意味に影響しない変更（空白、フォーマットなど）
- `refactor`: バグ修正でも機能追加でもないコード変更
- `test`: テストの追加・修正
- `chore`: ビルドプロセスやツールの変更

**例:**
```
feat: Add user profile page
fix: Correct authentication redirect issue
docs: Update README with deployment instructions
```

### 3. プルリクエスト

1. **ブランチを作成**
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. **コードを書く**
   - コーディング規約を守る（Laravel Pint使用）
   - テストを追加・更新
   - ドキュメントを更新

3. **品質チェック**
   ```bash
   # コードスタイル
   ./vendor/bin/pint
   
   # 静的解析
   ./vendor/bin/phpstan analyse
   
   # テスト
   composer test
   ```

4. **コミット＆プッシュ**
   ```bash
   git add .
   git commit -m "feat: Add your feature"
   git push origin feature/your-feature-name
   ```

5. **プルリクエストを作成**
   - GitHubでプルリクエストを作成
   - レビュアーを指定
   - CIが通過するのを待つ

## コーディング規約

### PHP / Laravel

- **Laravel Pint** でコードスタイルを統一
  ```bash
  ./vendor/bin/pint
  ```

- **PHPStan** で静的解析（レベル5）
  ```bash
  ./vendor/bin/phpstan analyse
  ```

### JavaScript / Vue

- **Prettier** + **ESLint** で統一
  ```bash
  npm run lint
  ```

### Git Hooks

プロジェクトは **Husky** + **lint-staged** を使用しており、コミット時に自動でコード品質チェックを実行します。

## テスト

### テストの実行

```bash
# 全テスト
composer test

# 特定のテスト
php artisan test --filter=ProfileTest

# カバレッジ
php artisan test --coverage
```

### テストの追加

- **Feature tests**: `web-app/tests/Feature/`
- **Unit tests**: `web-app/tests/Unit/`

## ディレクトリ構造

```
ai-diary/
├── infrastructure/     # Docker設定（本番環境）
│   ├── docker-compose.yml
│   ├── Dockerfile
│   └── nginx/
└── web-app/           # Laravelアプリケーション
    ├── app/
    ├── resources/
    └── tests/
```

## バグ報告

バグを発見した場合は、以下を含めてIssueを作成してください：

- **環境情報**: OS、Dockerバージョンなど
- **再現手順**: ステップバイステップで記載
- **期待される動作**
- **実際の動作**
- **スクリーンショット**: 該当する場合

## 機能リクエスト

新機能のアイデアがある場合は、Issueで提案してください：

- **ユースケース**: なぜこの機能が必要か
- **提案する実装方法**: 可能であれば
- **代替案**: 検討した他のアプローチ

## 質問・サポート

- **ドキュメント**: [README.md](README.md) を最初に確認
- **Issue**: 技術的な質問はIssueで
- **Discussions**: アイデアや議論はDiscussionsで

## ライセンス

貢献されたコードは [MIT License](LICENSE) の下でライセンスされます。

---

貢献していただきありがとうございます！ 🎉
